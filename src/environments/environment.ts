export const environment = {
  production: false,
  // storage key used locally for auth persistence
  authKey: 'auth_v1',
  // default API base (kept as reqres for demo). Change to your backend when available.
  apiBase: 'https://reqres.in/api',

  // Firebase REST API settings (leave apiKey empty for now)
  firebaseApiKey: '',
  firebaseAuthBase: 'https://identitytoolkit.googleapis.com/v1'
};
